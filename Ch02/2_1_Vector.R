# 날짜 : 2021-06-28
# 이름 : 박재형
# 내용 : Vector 자료구조 - p58

# vector = R에서 가장 많이 사용하는 자료구조, 배열과 동일하다. index번호는 1번부터
v1 <- c(1, 2, 3, 4, 5)
v1
v1[1]

v2 <- c(1:5)
v2

# 벡터 연산
x <- c(1:4)
y <- c(5:8)

x+y
x-y
x*y

# seq() 함수로 벡터생성
v3 <- seq(1, 10, by=2)  # 1부터 10까지 2씩 증가
v3

# 벡터의 합
sum_v1 <- sum(v1)
sum_v2 <- sum(v2)
sum_v1
sum_v2

# 벡터의 평균
mean_v1 <- mean(v1)
mean_v1























